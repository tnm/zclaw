#ifndef ESP_HTTP_CLIENT_H
#define ESP_HTTP_CLIENT_H

/*
 * Minimal host-test stub for esp_http_client API.
 * The LLM runtime tests compile llm.c in stub-LLM mode, which does not execute
 * HTTP client paths, so only placeholder types are needed.
 */

typedef void *esp_http_client_handle_t;

#endif // ESP_HTTP_CLIENT_H
